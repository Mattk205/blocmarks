<h1><%= @bookmark.url %></h1>
<div class="col-md-4">
  <%= link_to "Edit Bookmark", edit_topic_bookmark_path(@bookmark), class: 'btn btn-success' %>
  <%= link_to "Delete Bookmark", topic_bookmark_path(@bookmark), method: :delete, class: 'btn btn-danger', data: { confirm: 'Are you sure you want to delete this wiki?' } %>
</div>
